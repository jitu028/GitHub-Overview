name: first workflow

on:
 workflow_dispatch

jobs:
 firstjob:
  runs-on: self-hosted
  steps:
    - name: firstjob script
      shell: bash
      run: echo Hello World! this is firstjob

 secondjob:
  runs-on: ubuntu-latest
  steps:
    - name: secondjob script
      shell: bash
      run: echo Hello World! this is secondjob

  
      
